# Supabase client-side configuration
# These are safe to expose in the client bundle (anon key only).
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Plausible analytics (optional)
PUBLIC_PLAUSIBLE_DOMAIN=emadram.github.io
